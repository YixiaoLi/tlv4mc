{
	"\[(?<time>\d+)\] dispatch to task (?<id>\d+)\." : [
		{
			"time"		:"${time}",
			"subject"	:null,
			"object"	:"task(id==${id})",
			"behavior"	:"dispatch"
		}
	],
	"\[(?<time>\d+)\] task (?<id>\d+) becomes (?<state>\w+)\." : [
		{
			"time"		:"${time}",
			"subject"	:null,
			"object"	:"task(Id==${id})",
			"behavior"	:"state=${state}"
		}
	]
}
