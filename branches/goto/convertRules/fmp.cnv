{
	"fmp":
	{
        [16950701]:[1]: task 1 migrates from processor 1 to processor 2.
        :[\d+]:
        
		"\[(?<time>\d+)\]:[\d+]: task (?<tskid>\d+) migrates from processor (?<fromPrcId>\d+) to processor (?<toPrcId>\d+)\." :
		{
            "$EXIST{Task(id==${tskid})}" :"[${time}]$RES_NAME{Task(id==${tskid})}.prcId = ${toPrcId}"
        }
    }
}
